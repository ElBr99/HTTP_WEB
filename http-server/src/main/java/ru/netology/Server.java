package ru.netology;

// интерфейс нужен тогда, когда несколько классов в теории выполняют одни и те же функции, поэтому чтобы не переписывать один и тот же метод несколько раз
public interface Server {

    void start();
}
